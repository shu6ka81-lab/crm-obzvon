/**
 * Ширины колонок таблицы расчёта — в отдельном обычном модуле.
 *
 * Раньше константа лежала рядом с формой строки, в файле с 'use client'.
 * Клиентский модуль на сервере подменяется ссылкой, и значение приходило
 * пустым: строки выстраивались правильно, а шапка над ними складывалась
 * в столбик. Симптом выглядит как ошибка вёрстки, причина — в другом слое.
 */
export const GRID =
  'grid grid-cols-[2rem_minmax(12rem,1fr)_4rem_5rem_5.5rem_4rem_5.5rem_5.5rem_6rem_5rem] items-center gap-2'
