# Скрипты пишутся в Windows, а выполняются на сервере. С переводами строк
# Windows bash их не запускает: ошибка вида «\r: command not found».
# Поэтому в репозитории и при выгрузке они всегда с переводами строк Unix.
*.sh text eol=lf
